node_modules/
dist/
out/
build/*
!build/icon.icns
.vite/
.dev/
.context/
.DS_Store
.env
package-lock.json
bun.lock
tests/agent/results/

# editor / tool MCP configs (auto-generated, user-specific)
.codex/
.cursor/
.gemini/
.mcp.json
.vscode/
.zed/
opencode.json
config/mcporter.json

# scratch artifacts (dogfood runs, manual captures, regression snapshots, ad-hoc logs)
tmp/
tmp-page*.png
dogfood-output/
regression-screenshots/
.afk-logs/

# Claude Code runtime state (active process lock — not user content)
.claude/scheduled_tasks.lock

.fallow/

# Node compile cache (generated by pnpm install)
node-compile-cache/

# AFK: local scratch, never commit (swept in by mistake in #250)
experiments/
harness/

# Fetched at build time by scripts/fetch-agent-browser.sh (pinned binary +
# upstream 'core' skill vendored at the same tag)
resources/bin/agent-browser
resources/skills/core/
