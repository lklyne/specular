packages:
  - '.'
  - 'packages/*'

nodeLinker: hoisted

overrides:
  '@electron/notarize': ^3.1.1

# @electron/rebuild resolves @electron/node-gyp from git; it has no npm release
blockExoticSubdeps: false

allowBuilds:
  electron: true
  esbuild: true
  macos-alias: true
  fs-xattr: true
